[build]
  command = "npm run build --prefix web"
  publish = "web/dist"

[dev]
  command = "npm run dev --prefix web"
  publish = "web/dist"
  port = 5173

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
